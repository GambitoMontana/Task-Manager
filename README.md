# Task Manager — Demo Website

Sitio demo para presentar el proyecto "Task Manager". Contiene `index.html` y `style.css`.

Instrucciones:

- Para ver localmente:
  - Abre `index.html` en tu navegador o usa Live Server.
- Para subir a GitHub:
  - Crea un repositorio y sigue los comandos proporcionados por el asistente.
